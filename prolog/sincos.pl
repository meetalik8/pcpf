calc:-
    write("Enter the angle in radians"),nl,
    read(X),
    Y is cos(X),
    Z is sin(X),
    write("The cos of the angle is:"),nl,
    write(Y),nl,
    write("The sin of the angle is:"),nl,
    write(Z).
